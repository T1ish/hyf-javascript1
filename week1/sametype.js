/*This is a program that test 2 variables and see if their types are the same. If they are "Same type" is displayed to the console. */

let x = 2;
let y = "String";

console.log("x = " + x);
console.log("y = " + y);

if (typeof(x) === typeof(y)){
   console.log("Same Type");
   }
